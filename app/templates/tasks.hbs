{{outlet}}
<h1>Tasks</h1>
{{#each model as |task|}}
    <div class="well">
      <h4>{{#link-to 'tasks.edit' task.id}}{{task.title}}{{/link-to}}</h4>
      <small>Date :{{format-date task.date}}</small>
      <p>{{task.description}}</p>
      <button {{action 'deleteTask' task.id}} class="btn btn-danger">Delete task</button>
    </div>
{{/each}}
